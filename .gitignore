# dependencies
node_modules/

# BaseHalf derived cache (rebuildable; the rest of .bh/ stays in git so
# canvas positions / badge prompts / references travel with the folder)
.bh/cache/

# build output
dist/
build/
.turbo/
*.tsbuildinfo

# logs / env / OS cruft
*.log
.env
.env.*
.DS_Store

# internal/strategy docs — never commit (kept locally only)
private-docs/
