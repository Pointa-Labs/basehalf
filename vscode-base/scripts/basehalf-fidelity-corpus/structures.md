# Heading one

## Heading two

### Heading three with `inline code`

A paragraph with **bold**, *italic*, ~~strikethrough~~, and a [link](https://example.com/a?b=c#d).

> A blockquote with **bold** text.
>
> Second paragraph of the same quote.

- unordered item one
- unordered item two
  - nested item
    - deeper nested item
- back to top level

1. ordered one
2. ordered two
   1. nested ordered
3. ordered three

- [ ] open task
- [x] done task

```ts
const x: number = 1;
function greet(name: string): string {
	return `hi ${name}`;
}
```

| Column A | Column B | Column C |
| -------- | :------: | -------: |
| a1       |    b1    |       c1 |
| a2       |    b2    |       c2 |

---

Final paragraph after a thematic break.
