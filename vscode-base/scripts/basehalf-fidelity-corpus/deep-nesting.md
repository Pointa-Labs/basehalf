- level 1
  - level 2
    - level 3
      - level 4 with `code`
        1. ordered inside deep bullet
        2. second
  - level 2 again

> outer quote
> > nested quote
> > > third level quote
>
> back to outer

1. ordered with paragraph

   A paragraph inside a list item.

   ```js
   const inList = true;
   ```

2. second item
