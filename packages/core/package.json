{
  "name": "@basehalf/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "BaseHalf core: kernel (registry + context) and the modules that register commands. The one door.",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b && tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check src test",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "22",
    "tsup": "8.3.5",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
