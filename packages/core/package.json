{
  "name": "@basehalf/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "BaseHalf legacy core: command registry backend and transitional modules during the VS Code-aligned service split.",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b && tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check src test",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "yaml": "^2.7.0"
  }
}
