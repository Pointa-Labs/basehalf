# dependencies
node_modules/

# the local knowledge store (user data — never commit it)
.bh/

# build output
dist/
build/
.turbo/
*.tsbuildinfo

# logs / env / OS cruft
*.log
.env
.env.*
.DS_Store

# internal/strategy docs — never commit (kept locally only)
private-docs/
